import { InfoWindowDirective } from './info-window.directive';

describe('InfoWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoWindowDirective();
    expect(directive).toBeTruthy();
  });
});

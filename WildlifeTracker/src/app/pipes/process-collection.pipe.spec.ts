import { ProcessCollectionPipe } from './process-collection.pipe';

describe('ProcessCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});
